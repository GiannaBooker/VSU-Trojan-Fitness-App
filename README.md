# VSU-Trojan-Fitness-App

This repo now includes the backend for:

- Login / logout (JWT-based email+password auth)
- Check-in / check-out (records timestamps + status)

See `backend/README.md` for setup and API usage.
